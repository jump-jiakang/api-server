{
  "name": "api-server",
  "version": "1.0.0",
  "description": "1. 将 `素材` 目录下的 `assets` 和 `home` 文件夹，拷贝到 `code` 目录下\r 2. 在 `code` 目录下新建 `login.html` 和 `index.html` 页面",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1"
  }
}
